- hosts: app
  become: true
  collections:
    - community.docker
  tasks:
    - name: Install required packages
      apt:
        name:
          - docker.io
          - python3-pip
        update_cache: yes
        state: present

    - name: Install Docker SDK for Python
      pip:
        name: docker

    - name: Pull Docker image
      community.docker.docker_image:
        name: "pborade90/myapp:{{ lookup('env','GIT_COMMIT') }}"
        source: pull

    - name: Run container
      community.docker.docker_container:
        name: myapp
        image: "pborade90/myapp:{{ lookup('env','GIT_COMMIT') }}"
        state: started
        restart_policy: unless-stopped
        ports:
          - "80:3000"
